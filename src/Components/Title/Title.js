import React from "react"
import { Title as StyledTitle } from './style'

function Title() {
    return (
        <StyledTitle>
          This is a title
        </StyledTitle>
    );
  }

export default Title;